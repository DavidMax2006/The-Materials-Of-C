#include <stdio.h>

int main(int argc, char const *argv[]) {
  int x, y, sum;   //Create 3 volumes
  x = 100;
  y = 200;
  sum = x + y;

  printf("sum is %d\n", sum);   //Print last result
  return 0;
}
